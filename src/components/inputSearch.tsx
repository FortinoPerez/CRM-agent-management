"use client"

import { Input } from "@/components/ui/input"

export const InputSearchCredit = () => (
    <Input
      placeholder="Buscar"
      id="search"
      name="search"
      type="search"
    />
)